services:
  mysql:
    image: mysql:8.0
    container_name: windemiko_mysql_dev
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: devpassword
      MYSQL_DATABASE: windemiko_dev
      MYSQL_CHARSET: utf8mb4
      MYSQL_COLLATION: utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --default-authentication-plugin=mysql_native_password
    volumes:
      - dev_mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-pdevpassword"]
      interval: 10s
      timeout: 5s
      retries: 5

  # PostgreSQL 备选（需要时取消注释）
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: windemiko_postgres_dev
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_PASSWORD: devpassword
  #     POSTGRES_DB: windemiko_dev
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - dev_pg:/var/lib/postgresql/data

volumes:
  dev_mysql:
  # dev_pg:
